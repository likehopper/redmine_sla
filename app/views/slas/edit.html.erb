<% html_title(l(:sla_label_global_settings)) %>
<div class="content"> 
  <div class="contextual">
    <%= link_to l('sla_label.sla.plural'), slas_path, class: 'icon icon-cancel' %>
  </div>
  <div class="title-sla-settings title-sla">
    <%= title l('sla_label.sla.edit') %>
  </div>  
  <%= render partial: 'form', locals: { sla: @sla } %>
</div>
<% content_for :sidebar do %>
  <%= render partial: 'slas/sidebar' %>
<% end %>