<% html_title(l(:sla_label_global_settings)) %>
<div class="content"> 
  <%= render :partial => 'action_menu' %>
  <div class="title-sla-settings title-sla">
    <%= title l('sla_label.sla.show') %>
  </div>  
  <h3><%= @sla.name %></h3>
</div>
<% content_for :sidebar do %>
  <% render partial: 'slas/sidebar' %>
<% end %>
