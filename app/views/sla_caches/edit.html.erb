<% html_title(l(:sla_label_global_settings)) %>
<div class="content"> 
  <div class="contextual">
    <%= link_to l('sla_label.sla_cache.plural'), sla_caches_path, class: 'icon icon-cancel' %>
  </div>
  <div class="title-sla-settings title-sla_cache">
    <%= title l('sla_label.sla_cache.edit') %>
  </div>  
  <%= render partial: 'form', locals: { sla_cache: @sla_cache } %>
</div>
<% content_for :sidebar do %>
  <%= render partial: 'slas/sidebar' %>
<% end %>