api.array :sla_calendar_holidays, api_meta(:total_count => @entity_count, :offset => @offset, :limit => @limit) do
  @entities.each do |entity|
    api.entity do
      api.id entity.id
      api.sla_calendar entity.sla_calendar
      api.sla_holiday entity.sla_holiday
    end
  end
end