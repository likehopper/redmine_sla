<% html_title(l(:sla_label_global_settings)) %>
<div class="content"> 
  <div class="contextual">
    <%= link_to l('sla_label.sla_schedule.plural'), sla_schedules_path, class: 'icon icon-cancel' %>
  </div>
  <div class="title-sla-settings title-sla_schedule">
    <%= title l('sla_label.sla_schedule.edit') %>
  </div>  
  <%= render partial: 'form', locals: { sla_schedule: @sla_schedule } %>
</div>
<%= render partial: 'slas/sidebar' -%>