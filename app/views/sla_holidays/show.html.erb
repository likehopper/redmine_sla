<% html_title(l(:sla_label_global_settings)) %>
<div class="content"> 
  <div class="contextual">
    <%= render :partial => 'action_menu' %>
  </div>
  <div class="title-sla-settings title-sla_holiday">
    <%= title l('sla_label.sla_holiday.show') %>
  </div>

  <h3>n°<%= @sla_holiday.id %>&nbsp;:&nbsp;<%= @sla_holiday.name %></h3>

  <p><%= @sla_holiday.date %></p>

</div>
<% content_for :sidebar do %>
  <% render partial: 'slas/sidebar' %>
<% end %>