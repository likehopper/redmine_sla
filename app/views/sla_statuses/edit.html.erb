<% html_title(l(:sla_label_global_settings)) %>
<div class="content"> 
  <div class="contextual">
    <%= link_to l('sla_label.sla_status.plural'), sla_statuses_path, class: 'icon icon-cancel' %>
  </div>
  <div class="title-sla-settings title-sla_status">
    <%= title l('sla_label.sla_status.edit') %>
  </div>  
  <%= render partial: 'form', locals: { sla_status: @sla_status } %>
</div>
<%= render partial: 'slas/sidebar' -%>