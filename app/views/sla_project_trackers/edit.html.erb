<% html_title(l('sla_label.sla_project_tracker.edit')) %>
<%= title l('sla_label.sla_project_tracker.edit') %>
<%= labelled_form_for([@sla_project_tracker],url: project_sla_project_tracker_path(Project.find(params[:project_id])), html: { multipart: false, id: 'sla_project_tracker_form', class: 'tabular', remote: request.xhr? }) do |f| %>
  <%= render partial: 'form', locals: { sla_project_tracker: @sla_project_tracker, f: f } %>
<% end %>