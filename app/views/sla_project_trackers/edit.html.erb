<% html_title(l('sla_label.sla_project_tracker.edit')) %>
<%= title l('sla_label.sla_project_tracker.edit') %>
<% Rails.logger.debug "Arguments: back_url = #{back_url}" %>
<%= labelled_form_for([@sla_project_tracker],
      url: _sla_project_tracker_path(@project, back_url: @back),
      html: { multipart: false, id: 'sla-project-tracker-form', class: 'tabular', remote: request.xhr? }) do |f| %>
  <%= render partial: 'form', locals: { sla_project_tracker: @sla_project_tracker, f: f } %>
<% end %>